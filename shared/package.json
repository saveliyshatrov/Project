{
  "name": "shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc --outDir dist",
    "dev": "nodemon --watch './src/**/*' --ext ts --exec \"npm run build && cd .. && npm install shared && cd server && npm install shared\""
  },
  "dependencies": {
    "nodemon": "^3.1.14"
  }
}
